# Module boundaries

Member, Operator, Doctor, and Image Gateway are modules in one application.
Cross-module work uses local application contracts or versioned domain events;
it does not use a network hop or duplicated module credentials.
